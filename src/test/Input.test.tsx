import React from "react";
import { render } from "@testing-library/react";
import Input from "../components/Inputs/Input";

test("renders learn react link", () => {
  render(<Input children={undefined} label={""} className={undefined} />);
});
